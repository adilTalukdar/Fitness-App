package com.fitness.userservice.model;

//created ENUM
//Enum is a special data type used to define a fixed set
// of constants. It improves type safety by restricting
// a variable to predefined values.

public enum UserRole {
    USER, ADMIN
}
